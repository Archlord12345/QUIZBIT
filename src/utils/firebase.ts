export const firebaseEnabled = false;
export const app = null;
export const auth = null;
export const db = null;

export const firebaseServerOnlyMessage =
  'Firebase est accessible depuis l app via les routes API Vercel uniquement.';
